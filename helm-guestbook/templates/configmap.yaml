apiVersion: v1
kind: ConfigMap
metadata:
    name: {{ include "helm-guestbook.fullname" . }}-config
    labels:
        {{- include "helm-guestbook.labels" . | nindent 4 }}
data:
    app.config: |
        # Application configuration
        debug: false
        environment: production